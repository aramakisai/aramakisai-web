# Implementation Plan

## スコープ変更 (2026-08-27)

RBAC に関する部分 (Requirement 9 / 10 / 12) は本スペックの対象外とし、`payload-cms-migration` スペックの Requirement 2 / 3 へ委譲した。

理由は Directus 12.1.1 の無償ライセンス (`CORE_LICENSE`) にある。`custom_permission_rules_enabled: false` のため、行レベルフィルタ・フィールド制限・`validation` を持つ `directus_permissions` 行が権限評価から無言で除外される。`student_exhibitor` ロールに求める「自企画のみ編集可」は Directus 上では実現できない。調査結果は research.md の「スパイク結果: Directus 12 の custom permission rule はライセンス機能」および「判断: RBAC は本スペックのスコープから外す」を参照。

Requirement 11 (画像の自動最適化) とその配信経路は実装と単体テストを完了しているが、リポジトリへのマージは保留している。拡張を動作させるには `aramakisai-infra` 側の Deployment 変更が必要で、Payload へ移行すれば不要になる配管のため、移行方針が確定するまで投入しない。実装は `payload-cms-migration` の Requirement 6 で同等機能を実現する際の参照とする。

マージしたのは Requirement 1〜8 および 13 に対応するスキーマ定義 (`directus/schema/snapshot.yaml`) のみで、2026-08-27 に prod / staging へ適用済み。

## 並行実装の前提

本スペックは **2 レーンが独立して進む**構造になっている。

- **レーン A (スキーマ / RBAC)**: `snapshot.yaml` → `directus/migrations/**`。DB スキーマに依存が集中する。
- **レーン B (拡張 / 配信経路)**: `directus/extensions/**` → `docker-compose.yaml` / `directus-schema-sync.yml`。スキーマにも RBAC にも依存しない。

両レーンは触るファイルが完全に分離しているため、最初から並行して着手できる。合流するのは最終の統合検証 (タスク 6) のみ。

### 実行ウェーブ

| Wave | 並行実行するタスク | 備考 |
|------|-------------------|------|
| 0 | 1.1 / 1.2 / 4.1 | 環境準備と前提実測、拡張の骨組み。3 つとも独立 |
| 1 | 2.1 → 2.2 → 2.3 (レーン A) ‖ 4.2 (レーン B) | レーン A の 3 つは同一ファイル (`snapshot.yaml`) を触るため直列。レーン B は並行 |
| 2 | 3.1 (スパイク) ‖ 5.1 | 3.1 の結果が 3.3 の設計を確定させる。5.1 は独立 |
| 3 | 3.2 / 3.3 / 3.4 ‖ 4.3 / 5.2 | RBAC の 3 本は別ファイルで並行可。ただし 3.5 は 3.2 の後 |
| 4 | 3.5 | `directus_permissions` の read 行を掃除するため、3.2 の read 更新後に実行する |
| 5 | 6.1 / 6.2 ‖ 6.4 | 実機確認。6.1 と 6.2 は対象が別 (権限 / 画像) で並行可 |
| 6 | 6.3 | 全変更が揃ってからのラウンドトリップと適用前チェック |

### 並行時の注意

- `directus_permissions` は 4 本の migration が共有する。各 migration の delete スコープ (policy × collection × action) が重ならないよう厳守する。重なるのは 3.2 (student_exhibitions の read) と 3.5 (ホワイトリスト外 read の削除) の 1 箇所のみで、3.5 のホワイトリストに `student_exhibitions` を含めることで衝突を回避する。
- migration ファイルの連番サフィックスは着手前に予約する (A: fields+read / B: files / C: junction / D: collection scope)。並行で採番すると衝突する。
- レーン B は Directus が起動していれば動くが、4.2 の実機確認には画像アップロードが必要なため、レーン A のスキーマ適用を待たずに既存コレクション (`topics` 等) の画像で検証してよい。

---

- [x] 1. 基盤: ローカル検証環境と破壊的変更の前提確認

- [x] 1.1 ローカル Directus に拡張の実行環境を用意する
  - `directus/extensions/hooks/sentry-error-tracking/` (`node_modules` のみが残る幽霊ディレクトリ) を削除する
  - `docker-compose.yaml` に拡張ディレクトリのマウントを追加する。既存の migrations マウントおよび `MIGRATIONS_PATH` と同じパスにしないこと
  - 空の拡張フォルダを置いた状態で Directus を起動し、拡張の探索対象パスが意図どおりで、既存の migrations マウントが拡張として誤検出されないことを起動ログで確認できる
  - _Requirements: 11.17, 11.20_

- [x] 1.2 (P) 本番・ステージングの実データ件数を実測する
  - prod / staging 双方の学生企画コレクションの行数を実測する。ステージングは停止していることがあるため起動してから確認する
  - 両環境とも 0 件であることを確認し、破壊的変更 (カラム削除・型変更) を実施してよい前提が成立していることを記録する
  - 0 件でない場合は破壊的変更を保留し、要件フェーズに差し戻す判断を明記する
  - _Requirements: 13.8_
  - _Boundary: Migration Strategy_

- [x] 2. スキーマ定義の刷新

  > 2.1〜2.3 は同一ファイル (`snapshot.yaml`) を編集するため直列に実行する。

- [x] 2.1 学生企画の入力フィールドを実運用に合わせる
  - 必須の団体名フィールドを新設し、既存の企画名フィールドの説明を「団体名は別フィールド」と分かる内容に改める
  - 未使用のスラッグフィールドを定義から削除する
  - カテゴリを複数選択できる配列型に変更し、選択値は変えずに表示テキストのみ「展示」「出店」に改め、既定値と「最大 2 つ」の注記を設定する。旧来の不要な説明文を削除する
  - リンクと SNS を登録する繰り返し入力のフィールドを、既存の同種フィールドと同じ構成で追加する
  - 区画・ブース関連 3 フィールドの表示条件を撤去して常時表示にし、用途のヒントを注記に残す
  - コレクションの日本語表示名を「学生企画」に変更する。コレクション名・テーブル名は変更しない
  - 編集後のスキーマ定義が構文として妥当で、上記すべての変更が 1 ファイルに収まっている
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 3.1, 3.2, 3.3, 3.4, 3.5, 4.1, 4.2, 6.1, 6.2, 8.1, 8.2_

- [x] 2.2 複数画像とステージ出演枠の逆参照を定義する
  - 単一画像フィールドを削除し、複数画像を扱う参照フィールドに置き換える。「最大 5 枚」は注記のみとする
  - 学生企画とファイルを結ぶ中間コレクションを、既存の同種中間コレクションと同じ 4 フィールド構成で新設し、2 本の関連に連鎖削除を設定する
  - ステージ出演枠への逆参照フィールドを追加し、既存の関連定義に逆参照名を紐付ける。カテゴリの値に依存しない常時表示とする
  - 中間コレクションは管理画面のナビゲーションに出さない設定にする
  - 学生企画レコードを削除すると中間コレクションの行が連鎖削除されることを、適用後のスキーマ定義上で確認できる
  - _Requirements: 5.1, 5.2, 5.3, 7.1, 7.2, 7.3, 7.4, 7.5_

- [x] 2.3 ローカル Directus にスキーマを適用して差分を目視確認する
  - 差分プレビューを確認したうえで適用する。カラム削除と型変更が意図どおりで、想定外のテーブル再作成が起きていないことを確認する
  - 適用後のテーブル定義で、スラッグカラムが物理削除されていること、新設カラム・中間テーブルの外部キーと連鎖削除を確認する
  - スキーマ用のカスタムマイグレーションを追加していないことを確認する
  - 想定外の差分が出た場合はロールバック判断の根拠として記録する
  - _Requirements: 2.2, 13.1, 13.5, 13.6, 13.7_

- [-] 3. 権限データの是正 (スコープ外に変更。research.md の「判断: RBAC は本スペックのスコープから外す」参照)

- [x] 3.1 関係越しのファイル参照条件を実証する
  - 中間コレクションを経由してファイルを絞り込む権限フィルタが、実際に権限評価で機能するかをローカルで検証する
  - 機能する場合は 3.3 の read 条件をその形で確定する
  - 機能しない場合は「自分がアップロードしたファイルのみ」に縮退させ、実行委員の代理アップロード分が学生の編集画面に出ない前提へ要件を改める判断を記録する
  - どちらの結論になったかが記録され、3.3 の実装方針が確定している
  - _Requirements: 10.7_
  - _Depends: 2.3_
  - _Boundary: RBAC File Policy_

- [-] 3.2 (P) 学生の編集可能フィールドと自レコードの参照範囲を是正する (スコープ外)
  - 学生ロールの作成・更新で扱えるフィールド一覧を、新しいスキーマのフィールド名に更新する。現行スキーマに存在しないフィールドを除去する
  - 更新は自分が作成したレコードに限る条件を維持する
  - 参照条件を「公開済み」または「自分が作成したもの」の二者択一に広げ、下書きを作成した本人が自レコードを開けるようにする
  - 対象ポリシー・対象コレクションのみを削除してから挿入する形にし、再実行しても同じ最終状態になる
  - 学生ユーザーで下書きの自レコードを開けることが、適用後に確認できる
  - _Requirements: 2.3, 4.3, 9.1, 9.2, 9.3, 9.4, 9.5_
  - _Depends: 2.3_
  - _Boundary: RBAC Field Policy_

- [-] 3.3 (P) ファイルの種別と可視範囲を制限する (スコープ外)
  - 学生ロールのアップロードを画像 3 種のみに限定する検証条件を設定する。アニメーション画像形式は含めない
  - 参照条件を 3.1 で確定した形にする。更新・削除は自分がアップロードしたファイルのみに限定する
  - ファイルサイズの条件は設定しない。書き込み後にしか値が確定せず機能しないことを migration のコメントに残す
  - 匿名アクセス用ポリシーと実行委員ポリシーのファイル権限は変更しない
  - 学生ユーザーで、他団体のファイルが一覧に出ないことが適用後に確認できる
  - _Requirements: 10.1, 10.2, 10.7, 10.8_
  - _Depends: 2.3, 3.1_
  - _Boundary: RBAC File Policy_

- [-] 3.4 (P) 中間コレクションの権限を新設する (スコープ外)
  - 実行委員に全操作権限を付与する
  - 学生ロールには、自分が作成した学生企画に紐づく行のみを対象とする条件で全操作を許可する
  - 対象コレクションのみを削除してから挿入する形にし、既存の同種中間コレクションの migration と同じ構成にする
  - 学生ユーザーが自企画に画像を紐付けでき、他企画の紐付けを操作できないことが適用後に確認できる
  - _Requirements: 10.9, 10.10_
  - _Depends: 2.3_
  - _Boundary: RBAC Junction Policy_

- [-] 3.5 学生ロールの参照対象コレクションを絞り込む (スコープ外)
  - 自企画の編集に必要なコレクションをホワイトリストとして持ち、それ以外の参照権限を削除する。削除対象を列挙する方式にはしない
  - ステージ出演枠は参照のみを維持し、作成・更新は付与しない
  - 匿名アクセス用ポリシーと実行委員ポリシーには手を入れない
  - ホワイトリストの意味と、将来コレクションを増やしたときの更新箇所を migration の冒頭コメントに明記する
  - 学生ユーザーの管理画面ナビゲーションから不要なコレクションが消え、自企画の編集画面では関連フィールドが引き続き解決されることが適用後に確認できる
  - _Requirements: 5.4, 12.1, 12.2, 12.3, 12.4, 12.5_
  - _Depends: 3.2_

- [x] 4. 画像最適化フックの実装 (実装完了・マージ保留)

- [x] 4.1 (P) フックの骨組みとフォーマット判定を用意する
  - アップロード完了イベントを購読する拡張として宣言する。ネストしたディレクトリでは検出されないため、探索対象の直下にフォルダを置く
  - 対象フォーマットの判定を実装する。対象外のファイルは何もせずに素通しする
  - 処理の成否と対象ファイルの識別子をログに出す。失敗は警告以上の水準で出す
  - 拡張として認識され、任意のファイルをアップロードするとログに判定結果が出る
  - _Requirements: 11.5, 11.7, 11.15, 11.16, 11.17_
  - _Boundary: Image Optimize Hook_

- [x] 4.2 変換と実体の置き換えを実装する
  - Directus 内部の変換サービスに、長辺の上限・フォーマット・品質・拡大禁止を指定して変換ストリームを生成させる。画像処理ライブラリを拡張から直接読み込まない
  - ファイルサービスの置換モードで実体を差し替える。イベントの再発火を止めて無限ループを防ぐ
  - 変換に失敗した場合は元ファイルを保持したまま処理を終える。イベントハンドラの失敗はアップロード応答に影響しないことを前提とする
  - 上限を超える画像をアップロードすると、拡張子・MIME・ファイルサイズ・寸法が置換後の実体と一致し、識別子で始まる余分なオブジェクトがストレージに残らない
  - _Requirements: 11.1, 11.2, 11.3, 11.4, 11.6, 11.8, 11.9, 11.10, 11.12_
  - _Depends: 4.1_

- [x]* 4.3 (P) フックの単体テストを追加する
  - 対象フォーマットの判定が、画像 3 種を対象・それ以外を素通しに分岐すること
  - 変換指定に長辺上限・品質・拡大禁止が正しく組まれること
  - 変換サービスが例外を投げた場合に置換を呼ばず、失敗として扱うこと
  - 置換呼び出しにイベント抑止の指定が渡ること
  - 上記 4 点がテストとして実行でき、すべて通る
  - _Requirements: 11.5, 11.6, 11.10_
  - _Depends: 4.2_
  - _Boundary: Image Optimize Hook_

- [x] 5. 配信経路の整備 (実装完了・マージ保留)

- [x] 5.1 (P) スキーマ同期ワークフローに拡張を載せる
  - 発火対象パス・差分判定・設定オブジェクト生成・コミット対象パスの 4 箇所すべてに拡張を追加する
  - 追跡対象のファイルのみを対象にし、未追跡ディレクトリを巻き込まない
  - 生成される設定オブジェクトが、探索対象の直下にサブディレクトリを作る形でファイルを配置する指定になっている
  - 自動生成される PR 本文の適用順序の記述が、実際のジョブの実行順序と一致するように修正する
  - ワークフローの構造テストが更新後の定義に対して通る
  - _Requirements: 11.18, 11.19_
  - _Boundary: Extension Delivery_

- [x] 5.2 インフラ側の受け入れ条件を確定する
  - 拡張のマウント先を探索対象の直下に変更する依頼を、5.1 で生成する設定オブジェクトの形と揃えて確定する
  - 変換の同時実行数を絞る設定と、変換対象の最大寸法の明示を依頼に含める
  - アップロードサイズ上限が適用済みであることを確認し、依頼から外す
  - 破壊的変更を含む PR であること、および PR テンプレートのチェックが偽になる旨の周知を含める
  - インフラ側の課題票に残作業が過不足なく反映され、本スペックのマージ条件として参照できる状態になっている
  - _Requirements: 10.3, 10.4, 10.5, 10.6, 11.13, 11.14, 11.18, 13.9, 13.10_
  - _Depends: 5.1_

- [x] 6. 統合検証

- [-] 6.1 (P) 権限の実機確認を行う (3 のスコープ外化に伴い不要)
  - 学生ユーザーで、下書きの自レコードを開けること、他人の下書きが見えないことを確認する
  - 許可外の MIME とアニメーション画像形式のアップロードが拒否されることを確認する
  - 自分のファイルと自企画に紐づく実行委員アップロード分が参照でき、他団体のファイルが参照できないことを確認する
  - ナビゲーションから不要なコレクションが消え、ステージ出演枠は参照のみで作成・編集の導線が出ないことを確認する
  - 実行委員ユーザーで全操作が従来どおり行えることを確認する
  - 権限テーブルを直接参照し、対象ポリシーの全行が意図どおりであることを確認できる
  - _Requirements: 9.4, 9.5, 10.1, 10.2, 10.7, 10.8, 10.9, 10.10, 12.3, 12.4, 12.5, 13.6_
  - _Depends: 3.3, 3.4, 3.5_
  - _Boundary: RBAC Field Policy, RBAC File Policy, RBAC Junction Policy, RBAC Collection Scope_

- [x] 6.2 (P) 画像最適化の実機確認を行う
  - 上限超過画像・上限以内画像・非画像ファイルのそれぞれで、変換の有無とメタデータの整合を確認する
  - 置換後にストレージへ余分なオブジェクトが残らないことを確認する
  - 変換対象の最大寸法を超える画像で、例外時に原本が保持されログから検知できることを確認する
  - 複数ファイルの同時アップロードで変換が設定どおり直列化され、実行環境のメモリ上限を超えないことを計測する
  - 上記 4 点の結果が記録され、インフラ側に依頼する設定値の妥当性を裏付けられる
  - _Requirements: 11.1, 11.2, 11.3, 11.4, 11.5, 11.6, 11.12, 11.13, 11.14_
  - _Depends: 4.2, 1.1_
  - _Boundary: Image Optimize Hook_

- [x] 6.3 スキーマのラウンドトリップと適用順序を確認する
  - スキーマを再出力し、リポジトリの定義と差分がないことを確認する
  - 本番と同じ順序 (スキーマ適用 → 標準マイグレーション → カスタムマイグレーション) でローカルに適用し直し、中間コレクションが作られてから権限が入ることを確認する
  - カスタムマイグレーションを再実行しても同じ最終状態になることを確認する
  - 本番のロールバック手段が打ち消しマイグレーションの追加であること、適用済みファイルの事後編集が反映されないことを、変更内容の説明に明記する
  - 差分ゼロと再実行の冪等性が確認できている
  - _Requirements: 13.1, 13.2, 13.3, 13.4, 13.6_
  - _Depends: 2.3, 3.5, 6.1_

- [x] 6.4 (P) 画像 URL の取得規約をフロントエンドへ申し送る
  - 実体の差し替えで URL が変わらないため、最適化前の実体がエッジに残りうることを記録する
  - 画像取得時に更新日時をクエリとして付与し、キャッシュエントリを分離する規約を明文化する
  - 申し送り内容がスペック上に残り、フロントエンドが本コレクションを参照し始める際の前提として参照できる
  - _Requirements: 11.11_
  - _Boundary: Image Optimize Hook_
