# Implementation Plan

期日: 2026-09-28 (全要件が対象)。実装対象は `cms/`。タスク 2 (aramakisai-infra 側の作業) は CMS のコードタスクと並行して進められる。CMS のローカル実装・テストはタスク 2 の完了を待たない。本番での送信と公開範囲の確認だけがタスク 2 を前提にする (各タスクの `_Depends:_` 参照)。

文言は design.md「文言表」の文字列をそのまま使い、表に無い文言を追加しない。招待リンクの有効期限は発行から 72 時間の定数、問い合わせ先 URL は環境変数 `EXHIBITOR_CONTACT_URL` で与える。

- [ ] 1. セキュリティ上の穴を塞ぐ
- [ ] 1.1 メディア配信エンドポイントでリクエスト元の read access を評価する
  - 配信エンドポイント (`/api/media/serve/:id/:size`) がメディアを引くときに access を評価させ、read access の外にあるメディアは存在しない場合と同じ 404 を返す
  - リダイレクト先のファイル配信は Payload 標準の read access 評価に任せ、access 制御を無効化する設定を入れない
  - 完了状態: read access が拒否されるメディアの配信要求が 404 になり、許可されるメディアは従来どおりリダイレクトされることを結合テストで確認できる
  - _Requirements: 7.11_
  - _Boundary: Media_
  - _Depends: 3.2_ (この時点までメディアの read は全員に許可されており、完了状態のテストが書けないため)
- [x] 1.2 (P) 荒牧祭SSO (OIDC) のグループ写像から学生団体を外す
  - OIDC のグループから学生団体ロールへの写像を削除し、学生団体グループだけの利用者はロール無しとして既存の拒否経路 (既存の拒否メッセージ、文言表 表B) に乗せる
  - ユーザーの新規作成は、OIDC から解決したロールが実行委員のときだけ行う (写像を外した後は学生団体ロールが解決されないため、このガードは保険として置く)
  - ログインボタンの文言を U-01「荒牧祭SSOでログイン」に変える
  - 完了状態: 学生団体グループだけの利用者でロール解決結果が無しになり、コールバックが既存の 403 を返してユーザーを作らないことを単体テストで確認できる
  - _Requirements: 5.1, 5.2_
  - _Boundary: RoleMapping, OidcCallback_
- [x] 1.3 (P) SSO のメールが学生団体アカウントと一致したとき、パスワードを差し替えて他のセッションを無効にする
  - 既存ユーザーの引き当て (識別子 → メールアドレス)、紐づけ、ロールを実行委員にする更新は現行のまま変えない。メールが学生団体アカウントと一致しても拒否しない (実行委員が入稿者を兼ねる可能性があるため)
  - 引き当てたユーザーの更新前のロールが学生団体だった場合は、同じ更新でパスワードを推測できない乱数に差し替える (`payload.update` の `data.password`)。さらにセッション発行 (`addSessionToUser`) の前に sessions を空にして、今回のログイン以外の既存セッションを全て無効にする。企画の所有者は変えない
  - 完了状態: 学生団体アカウントとメールが一致する SSO ログインで、ロールが実行委員になり、パスワードのハッシュが変わり、sessions が今回の 1 件だけになることを単体テストで確認できる。実行委員は従来どおりログインできる
  - _Requirements: 5.3, 5.4_
  - _Boundary: OidcCallback_

- [ ] 2. aramakisai-infra 側の作業 (別リポジトリ)
- [ ] 2.1 (P) CMS の Secret に送信専用アカウントの SMTP パスワードを追加する
  - `cms-secrets` の ExternalSecret に、Infisical の `NOREPLY_SMTP_PASSWORD` を参照する項目と、テンプレート側の出力を追加する
  - migrate の PreSync Job は `cms-secrets` しか読まない (`gitops/manifests/prod/cms/migrate-job.yaml:25-27`) ため、SMTP 関連の値は Deployment の env ではなく `cms-secrets` に入れる。2.2・2.4 と同じ変更でマージする
  - 完了状態: 本番の `cms-secrets` に `NOREPLY_SMTP_PASSWORD` が入り、CMS の Pod から環境変数として読める
  - このタスクを前提にするコードタスク: 7.1 (本番で SMTP 送信を有効にする)、8.5 (本番反映後の招待メール送信確認)
  - _Requirements: 4.7_
  - _Boundary: aramakisai-infra cms-secrets_
- [ ] 2.2 (P) CMS の Secret に SMTP 接続先を追加する
  - `cms-secrets` の ExternalSecret の `template.data` に、クラスタ内 docker-mailserver の Service を指す固定値 `SMTP_HOST` を追加する (migrate の PreSync Job でも読めるように、Deployment の env には置かない)
  - 完了状態: 本番の CMS Pod と migrate Job に `SMTP_HOST=mailserver.prod.svc.cluster.local` が設定される
  - このタスクを前提にするコードタスク: 7.1、8.5 (2.1 と同じ)
  - _Requirements: 4.7_
  - _Boundary: aramakisai-infra cms-secrets_
- [ ] 2.3 (P) S3 バケットのアップロード領域に匿名 GET ができないことを確かめる
  - Hetzner Object Storage の `payload-uploads/` 以下に対する匿名 GET が拒否されるか確認し、許可されていれば拒否に変える
  - 完了状態: 学生団体の未使用画像を、CMS を経由せずバケットの URL から直接取得できないことを確認済みである
  - このタスクを前提にするコードタスク: 1.1・3.2・6.1 による未認証の非公開化 (7.9) を本番で完結させる。コードの実装・テストは前提にしない
  - _Requirements: 7.9_
  - _Boundary: aramakisai-infra object storage_
- [ ] 2.4 (P) 問い合わせ先 URL を CMS に渡す
  - Infisical の prod に `EXHIBITOR_CONTACT_URL` を登録し、`cms-secrets` の ExternalSecret の `data` と `template.data` に追加する (migrate Job も読めるように Deployment の env には置かない)
  - 2.1・2.2 と同時に反映する (`SMTP_HOST` が入ると起動時に必須になるため)
  - 完了状態: 本番の CMS Pod と migrate Job で `EXHIBITOR_CONTACT_URL` が読める
  - このタスクを前提にするコードタスク: 7.3 (本番起動)、8.5
  - _Requirements: 4.4, 4.9_
  - _Boundary: aramakisai-infra cms-secrets_

- [x] 3. アクセス判定の土台を作る
- [x] 3.1 学生企画とユーザーの判定を「所有者本人のみ」へ絞る
  - 学生団体の学生企画: 読み取りは所有者が自分のもの、更新は所有者が自分かつ下書きのもの、作成・削除は不可にする
  - 未認証の学生企画の読み取りは公開状態のものだけのまま保つ
  - 学生団体のユーザー: 読み取り・更新は自分のレコードだけ、作成・削除は不可にする (アカウント画面でのパスワード変更に使う)
  - 自分のレコードを更新できるようになると、ロールとメールアドレスを書き換えられる。このため同じタスクで、ユーザーのロールとメールアドレス (上書き定義) の update を実行委員だけにする (`cms/src/collections/users.ts:31-38` のロール定義にフィールド access を足す)。3.4 のフィールド access を使う
  - 実行委員は全操作を許可したまま保ち、上記以外のコレクションの読み取りは現行の公開判定を維持する (エリア・出演枠の名前解決に使う)
  - 完了状態: 判定表の学生企画・ユーザーの全行を、実行委員・学生団体・未認証の 3 者で確かめる単体テストが通り、学生団体が自分のロール・メールアドレスを変える更新が反映されない
  - _Requirements: 1.1, 1.2, 1.3, 2.3, 3.5, 3.10, 4.17, 4.18, 4.19, 7.4, 7.5, 7.6, 10.2_
  - _Depends: 3.4_
- [x] 3.2 メディアの判定を所有者と公開企画での使用状況で分ける
  - 学生団体: 読み取りは自分が所有者のもの、作成は可、更新・削除は自分が所有者で公開企画で使用中でないもの
  - 未認証: 所有者なし、所有者が実行委員、公開企画で使用中のいずれかに当たるものだけ読める
  - 所有者なしの既存メディアは学生団体の一覧に出さず、未認証には従来どおり返す
  - 完了状態: 判定表のメディアの全行を 3 者で確かめる単体テストが通る
  - _Requirements: 7.9, 9.2, 9.3, 9.4, 9.5, 9.6, 9.7, 9.8, 10.2_
- [x] 3.3 学生団体の管理画面ナビを学生企画とメディアだけにする
  - 実行委員以外には、学生企画とメディア以外のコレクション・グローバルを管理画面のナビとダッシュボードで隠す判定を追加する
  - コレクションとグローバルの登録口で、この判定を管理画面の非表示設定に結線する
  - 完了状態: 許可リストの判定が単体テストで確認でき、学生団体でログインした管理画面のナビに学生企画とメディアだけが並ぶ
  - _Requirements: 2.1, 2.2, 2.4_
- [x] 3.4 フィールド単位の「実行委員のみ」「全面禁止」の access を用意する
  - 実行委員だけが対象操作を行えるフィールド access と、誰もフォーム・API から書けない (フックが特権で書く値に使う) フィールド access を追加する
  - 完了状態: 両者がロールごとに期待どおりの真偽を返すことを単体テストで確認できる
  - _Requirements: 3.6, 3.7, 3.8, 3.9, 4.18, 7.2, 7.3_

- [ ] 4. スキーマの追加とマイグレーション
- [ ] 4.1 メディアとユーザーに列を追加する
  - メディアに所有者 (ユーザーへの参照、任意、索引あり、ラベル F-42「アップロード者」) と公開企画での使用中フラグ (ラベル F-44「公開企画で使用中」) を追加する
  - ユーザーに招待状態 (ラベル F-55「招待メール」、選択肢 F-56「送信済み / 送信失敗」)、招待送信日時 (F-57「招待送信日時」)、送信エラー (F-59「送信エラー」)、列を持たない再送指示 (F-61「招待メールを再送」) を追加する
  - どの列にも DB の既定値を持たせず、既存のフィールドの削除・型変更・必須化をしない
  - `payload.config.ts` の `jobs` にタスク `sendInvitation` (入力はユーザー ID、`retries: 2`。処理の中身は 7.5) を登録し、`autoRun` を 10 秒間隔 (`*/10 * * * * *`) にする (`payload/dist/index.js:239-244`)。テスト中は `shouldAutoRun` を偽にする。`jobsCollectionOverrides` で `payload-jobs` の access を実行委員だけにする。これで `payload-jobs` テーブルがマイグレーションに入る
  - 完了状態: 追加したフィールドがコレクション・グローバルの定義に揃い、型チェックが通る
  - _Requirements: 4.10, 4.11, 9.1, 10.3_
- [ ] 4.2 マイグレーションを生成し型を再生成する
  - `pnpm migrate:create` で追加分のマイグレーションを生成し、`pnpm generate:types` で CMS 側と `frontend/src/cms-types.ts` の型を再生成する
  - 生成されたマイグレーションが、列・索引・外部キー (削除時は NULL にする) と、ジョブキューの `payload-jobs` テーブル (4.1) の追加だけであることを確かめる。S3 プラグインが注入する `media.prefix` の DROP 差分が混ざった場合は取り除き、ローカル起動で生じた importMap の差分はコミットに含めない
  - 完了状態: ローカル DB に `pnpm migrate` が通り、生成型に新しい列が追加されて既存の型が変わっていない
  - _Requirements: 10.3_
- [ ] 4.3 結合テストの実行環境を用意する
  - 結合テスト (`*.int.test.ts`) は `DATABASE_URL` と `PAYLOAD_SECRET` が無いと `describe.skipIf` でスキップされる。以降の結合テストのタスク (6.2, 8.2〜8.4) の前に、実際に走る状態を作る
  - `cms/` で `pnpm db:up` を実行し、環境変数をコマンドの前に付けて `pnpm migrate` と `pnpm test` を実行する (`.env` ファイルは使わない)
    - `DATABASE_URL=postgres://payload:payload@localhost:5433/payload PAYLOAD_SECRET=<任意の文字列> pnpm migrate`
    - `DATABASE_URL=postgres://payload:payload@localhost:5433/payload PAYLOAD_SECRET=<任意の文字列> pnpm test`
  - 完了状態: 既存の結合テスト (`access.int.test.ts` など) がスキップされずに実行されたことを、テスト出力の件数で確認できる
  - _Requirements: 10.2_
  - _Depends: 4.2_

- [ ] 5. 学生企画コレクションの制限と文言
- [ ] 5.1 所有者を実行委員が選べるようにし、重複と不正な指定を拒否する
  - 所有者を実行委員の管理画面に表示し、選択肢を学生団体ロールのユーザーに絞る。学生団体には表示も変更もさせない
  - 実行委員以外のときに既存値や自分を所有者に入れる現行の処理を外し、フィールド access と Local API の特権で経路の差を無くす
  - 保存前の検証で、同じ所有者を持つ別の企画があれば M-E02「{メールアドレス}は既に{団体名}の所有者です。」を、学生団体でないユーザーを指定したら M-E03「所有者に学生団体のアカウントを選んでください。」を、所有者フィールドのエラーとして返す
  - 完了状態: 実行委員が所有者・エリア・ブース番号を指定して作った企画がその値で保存され、重複指定が M-E02 で拒否される
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.6_
- [ ] 5.2 割当と公開状態を実行委員だけが変えられるようにする
  - マップ配置エリア・ブース番号・マップ表示ラベル・公開状態を、全員が読めて実行委員だけが書けるフィールドにする。学生団体の入力値は捨てて既存値を保つ
  - 公開状態をサイドバーに置き、一覧の既定列を団体名・カテゴリ・公開状態にして、状態の判別と絞り込みをできるようにする
  - 公開状態は「下書き」「公開」の 2 値のまま保つ
  - 画像欄は ID の形式しか検証されないため、学生団体が他団体の未公開画像の ID を自分の企画に保存できてしまう。保存前の検証 (`hooks/payload-constraints.ts` と同じ beforeValidate の方式) で、学生団体のリクエストのとき、保存前に無く新しく加わった画像 ID の所有者が本人であることを特権で引いて確かめ、違えば M-E17「自分がアップロードした画像だけを選べます。」を画像フィールドのエラーとして返す
  - 完了状態: 学生団体の管理画面で割当と公開状態が読み取り専用で表示され、実行委員は一覧を公開状態で絞り込め、どちらの向きにも公開状態を変えられる。学生団体が他人の画像 ID を指定した保存が M-E17 で拒否される
  - _Requirements: 3.7, 3.8, 3.9, 7.1, 7.2, 7.3, 7.7, 7.8, 7.12, 9.9_
- [ ] 5.3 学生団体が自分の公開済み企画を保存したときに案内付きで拒否する
  - 学生団体による更新要求のうち、対象が本人所有かつ公開状態のものを、access 評価より前に M-E01「公開中の企画のため、修正は実行委員に依頼してください。」の 403 で拒否する
  - 本人の企画でなければ何もせず、後段の access が拒否するのに任せる (他団体の公開状態を漏らさない)
  - 完了状態: 学生団体の権限で REST (`PATCH /api/student_exhibitions/:id`) から自分の公開済み企画を更新すると 403 と M-E01 が返り、内容が変わらない。管理画面では公開済み企画はドキュメント単位の権限で読み取り専用になるため (`payload/dist/collections/operations/docAccess.js:28`)、確認は REST で行う
  - _Requirements: 7.5_
- [ ] 5.4 画像の上限を検証し、学生団体向けの説明文を差し替える
  - 各カテゴリの画像が 6 枚以上なら M-E07「画像は最大5枚です。」で拒否する。`hooks/payload-constraints.ts` の既存フックと同じく、beforeValidate で `ValidationError` を投げる方式にする。`maxRows` は使わない
  - 説明文を文言表のとおりにする: 公開状態 F-05「公開は実行委員が行い、公開後は編集できません。」、画像 F-15「最大5枚まで。1枚目がサムネイルとして表示されます。」、ステージ出演枠 F-17「ステージ出演枠」、マップ配置エリア F-19「割り当てられた出店エリア」、ブース番号 F-21「割り当てられた出店グループ内の番号もしくは教室番号」、マップ表示ラベル F-23「割り当てられた出店エリア名」、リンクの URL F-27「https://から始まるURLを入力してください。」
  - 表 B の既存文言は変えない
  - 完了状態: 学生団体の編集画面の説明文に DB 用語・フィールド名が残らず、6 枚目の画像を付けた保存が M-E07 で拒否される
  - _Requirements: 8.1, 8.2, 8.3, 8.4, 8.5, 8.6_

- [ ] 6. メディアの所有者と公開企画での使用状況
- [ ] 6.1 (P) メディアに所有者を記録し、使用中の画像の変更を拒否する
  - アップロード時に、アップロードしたユーザーを所有者として、使用中フラグを「使用していない」として記録する
  - 所有者は実行委員だけが読み書きでき、使用中フラグは全員が読めて誰も直接書けないようにする
  - 学生団体が本人所有で使用中の画像を更新・削除しようとしたら、access 評価より前に M-E05「公開中の企画で使用中の画像は変更・削除できません。」の 403 で拒否する。ID 指定だけでなく、一覧からの一括操作 (where 指定) も対象にし、where で対象を特権で引いて判定する (一括削除は access の Where で使用中の画像を黙って外すため、判定しないと M-E05 が出ない。`payload/dist/collections/operations/delete.js:32, 46-57`)
  - 代替テキストの説明文を F-41「画像の内容を短い文で説明してください。画像読込み時にエラーが発生した場合などに表示されます。」にする
  - 完了状態: 学生団体がアップロードした画像に本人が所有者として記録され、使用中の画像の更新・削除が ID 指定でも where 指定でも M-E05 で拒否される
  - _Requirements: 8.5, 9.1, 9.4, 9.5, 9.6_
  - _Boundary: Media_
  - _Depends: 3.2, 3.4, 4.2_
- [ ] 6.2 (P) 公開企画での使用状況を再計算する処理を作る
  - 企画の 4 カテゴリの画像から画像 ID を集める処理と、指定した画像それぞれが公開中の企画から参照されているかを求めて使用中フラグに書き込む処理を作る
  - 企画の保存と同じトランザクション内で動き、何度実行しても同じ結果になるようにする
  - 使用中フラグの書き込みは where 指定の一括 update で行う。これは失敗を例外にせず戻り値の `errors` に入れて返す (`payload/dist/collections/operations/update.js:202-210`) ため、`errors` が空でなければ throw して企画の保存ごと巻き戻す
  - 完了状態: 公開中の企画から参照される画像だけが使用中になり、外れた画像が使用していない状態に戻ることを結合テストで確認できる
  - _Requirements: 7.9, 7.10, 9.5_
  - _Boundary: MediaPublicationSync_
  - _Depends: 4.2, 4.3_

- [ ] 7. 学生団体アカウントのローカル発行と招待メール
- [ ] 7.1 (P) SMTP 送信のメールアダプタを組み込む
  - `@payloadcms/email-nodemailer` (3.88.0) を依存に加え、`SMTP_HOST` が設定されているときだけ docker-mailserver へ 587/STARTTLS で接続するアダプタを有効にする。有効にするときは、S3 の変数と同じく `NOREPLY_SMTP_PASSWORD` を読み込み時に `requireEnv` する。`infisical run --env=prod` には `SMTP_HOST` が入らないため、ローカル起動はコンソール出力のまま動く
  - SMTP に短いタイムアウトを付ける (`connectionTimeout` / `greetingTimeout` / `socketTimeout` を各 10 秒)
  - 送信元とユーザー名を `noreply@aramakisai.com`、送信者名を M-05「荒牧祭実行委員会広報部」にし、TLS のサーバー名を証明書のホスト名に合わせる
  - 環境変数は既存の環境変数読み出しの仕組み経由で読む
  - ローカルのコンソール adapter は宛先と件名しか出力しない (`payload/dist/email/consoleEmailAdapter.js:9`)。ローカルでパスワード設定リンクを確かめる手順を `docs/cms-operations.md` のローカル開発の節に書く: ローカル DB で `SELECT reset_password_token FROM users WHERE email = '<宛先>'` を実行し、`http://localhost:3000/admin/reset/<トークン>` を開く
  - 完了状態: `SMTP_HOST` 未設定 (`infisical run --env=prod` を含む) でも CMS が起動し、設定時はアダプタが有効になる。本番で実際に送れるのはタスク 2.1・2.2・2.4 の反映後
  - _Requirements: 4.7_
  - _Boundary: payload.config_
- [x] 7.2 (P) 招待メールの件名と本文を作る
  - 件名を M-01「【荒牧祭】HP企画ページの入稿用アカウントのご案内」にする
  - 本文は M-02 本文をそのまま使い、パスワード設定リンク・ログイン画面の URL・問い合わせ先 URL を入力として受け取って差し込む。問い合わせ先 URL をテンプレートに直書きしない。改行は改行タグ、リンクはアンカーにし、有効期限は書かない
  - パスワード文字列を受け取る入力を持たない
  - 完了状態: 生成した本文に 3 つのリンクと M-02 の全文が含まれることを単体テストで確認できる
  - _Requirements: 4.3, 4.4, 4.6_
  - _Boundary: EmailTemplates_
- [ ] 7.3 (P) 問い合わせ先 URL を環境変数から読む
  - 既存の環境変数読み出し (`requireEnv`) で `EXHIBITOR_CONTACT_URL` を読む。SMTP 送信を有効にする環境 (`SMTP_HOST` あり) では、S3 の変数と同じく設定読み込み時に読み、未設定なら起動を失敗させる
  - 完了状態: `SMTP_HOST` があるときに未設定だと起動が失敗し、設定時は値を招待処理へ渡せる。本番の値はタスク 2.4 で与える
  - _Requirements: 4.4, 4.9_
  - _Boundary: payload.config, Invitation_
- [x] 7.4 (P) 招待リンクの有効期限を定数にする
  - 招待リンクの有効期限を、発行 (再送を含む) から 72 時間のミリ秒の定数として定義する
  - 完了状態: 定数が 72 時間であることを単体テストで確認できる
  - _Requirements: 4.8_
  - _Boundary: Invitation_
- [ ] 7.5 招待を commit 後に送るジョブを作り、結果をユーザーに記録する
  - users の afterChange・afterOperation はどちらも commit の前に走り (`payload/dist/collections/operations/create.js:291, 308, 324`)、commit 後に呼ばれるフックは無い。フック内でトークン発行や送信をすると、`forgotPassword` の失敗が作成のトランザクションを巻き戻したのに作成が成功を返す (`auth/operations/forgotPassword.js:150`、`collections/operations/updateByID.js:170`)。このため Payload のジョブキューを使う
  - 4.1 で登録したタスク `sendInvitation` の処理: 7.4 の期限で Payload の再設定トークンをメール送信なしで発行し、7.3 の問い合わせ先 URL を差し込んで 7.2 の件名・本文で送り、招待状態・送信日時・送信エラーを記録する (成功時はエラーを空にする)
  - トークン発行と送信の失敗は例外にせず、「送信失敗」と M-E11「送信に失敗しました: {エラーメッセージ}」を記録して正常終了する (4.10)。記録そのものの失敗は例外のまま投げ、ジョブの再試行に任せる
  - キュー投入 (7.7) の失敗は、作成・更新の失敗として扱う。ジョブの行は作成と同じトランザクションに入るため、作成が巻き戻れば送信もされない
  - 失敗はエラーログにユーザー ID と理由を出す
  - 完了状態: ジョブを実行すると送信成功で「送信済み」、送信失敗で「送信失敗」が記録され、再発行で以前のトークンが無効になる
  - _Requirements: 4.2, 4.4, 4.8, 4.10, 4.12_
  - _Depends: 4.2, 7.1, 7.2, 7.3, 7.4_
- [ ] 7.6 ユーザーのフィールド制限と、作成時のパスワード差し替えを入れる
  - メールアドレス・ロールの変更、OIDC 識別子の読み書き、招待記録の読み取りを実行委員だけにし、招待記録は誰も直接書けないようにする。ロールの読み取りは全員のまま保つ
  - 一覧の既定列をメールアドレス・ロール・招待メールにする
  - 学生団体ロール (未指定時の既定値を含む) の作成では、入力されたパスワードや省略を問わず、サーバー側で推測できない乱数に差し替える
  - 管理画面の新規作成では、ラベル・説明なしの ui フィールドに client component を置く。新規作成時 (ドキュメント ID が無いとき) だけ、パスワード欄と確認欄に同じ乱数を 1 回自動入力して、クライアント側の必須・一致検証を通す。画面には何も描画しない。
    - 実装: `useDocumentInfo()` の id が無いときだけ動かす。`useEffect` と `useRef` で 1 回だけ、`useForm().dispatchFields({ type: 'UPDATE', path: 'password', value })` と、同じ値の `path: 'confirm-password'` を呼ぶ (根拠: `@payloadcms/ui/dist/forms/Form/fieldReducer.js:388-405` の UPDATE は未登録のパスでも値を持つフィールドを作る。`forms/Form/index.js:616` で `dispatchFields` が form context に載る)
    -`payload generate:importmap` で importMap にこのコンポーネントのエントリだけを足す (ローカル起動で消える既存エントリの差分は含めない)
  - 完了状態:
    - メールアドレスだけを指定した API 経由の作成が成功する
    - 管理画面の新規作成で、パスワード欄と確認欄に何も入力せずに保存でき、招待メールが送られる
    - 学生団体が自分のロール・メールアドレスを変える保存が反映されない
  - _Requirements: 4.1, 4.5, 4.6, 4.18, 4.19_
  - _Depends: 3.1, 3.4, 7.7_ (招待メールの送信の確認に 7.7 が要る)
- [ ] 7.7 招待の自動送付・再送と、期限切れリンクの案内を結線する
  - 学生団体ユーザーの作成後 (afterChange) に、7.5 のジョブをキューに入れる。どの経路で作成しても同じように入れる
  - 実行委員が再送指示を付けて保存したら、リクエストの文脈にフラグを移し、保存後に消費してからキューに入れる (再設定トークン発行や記録の更新で再帰しないようにする)。対象が学生団体でなければ入れない
  - パスワード設定の要求で、トークンが存在しないか期限切れなら M-E06「リンクが無効なため、実行委員に招待メールの再送を依頼してください。」の 403 を返す。design.md の Users 節の `guardResetToken` (resetPassword の beforeOperation で `payload.db.findOne` を使ってトークンと期限を引く) のとおりに実装する
  - 再設定メールの件名・本文と有効期限はコレクション設定に入れず Payload 既定のまま使い、ログイン画面からの本人の要求でだけ送られる状態を保つ
  - 完了状態: 学生団体を作成してジョブを実行すると招待が 1 通送られ、再送で旧リンクが M-E06 になり、新しいリンクで設定したパスワードで学生団体としてログインできる
  - _Requirements: 4.2, 4.5, 4.11, 4.12, 4.13, 4.14, 4.15, 4.16_
  - _Depends: 7.5_

- [ ] 8. 結線と検証
- [ ] 8.1 企画の保存・削除時にメディアの使用状況を同期する
  - 学生企画の保存後と削除後に、保存前後いずれかが公開状態のときだけ、前後の画像 ID の和集合について 6.2 の再計算を呼ぶ
  - 完了状態: 実行委員が企画を公開すると参照される学生団体の画像が未認証で読めるようになり、下書きに戻すと読めなくなる
  - _Requirements: 7.9, 7.10, 9.5_
  - _Depends: 5.3, 6.2_
- [ ] 8.2 (P) 学生企画とユーザーのアクセス制限を結合テストで検証する
  - 学生団体の一覧・件数・他団体 ID の直接取得、下書きの保存、公開済みの保存 (M-E01)、割当と公開状態を変える保存が反映されないこと、作成の拒否を確かめる
  - ユーザーの本人 read/update と、ロール・メールアドレスの変更が反映されないこと、ユーザー作成の拒否を確かめる
  - 実行委員の所有者指定作成を、Local API (特権) と REST 相当 (ユーザー付き・access 評価あり) の両方で行い、同じ値と同じ重複エラーになることを確かめる
  - 学生団体の作成禁止に合わせ、既存の「所有者を省略・他者指定した作成で自分が所有者になる」テストを書き換える
  - 完了状態: 実行委員・学生団体・未認証の 3 者について、実 DB の結合テストが通る
  - _Requirements: 1.1, 1.2, 1.3, 3.2, 3.3, 3.4, 3.5, 3.8, 3.9, 3.10, 4.17, 4.18, 4.19, 7.3, 7.4, 7.5, 7.6, 10.2_
  - _Boundary: access.int.test_
  - _Depends: 4.3, 5.4, 7.6_
- [ ] 8.3 (P) メディアの公開範囲を結合テストで検証する
  - 企画の公開で学生団体の画像が未認証に読めるようになり、下書きに戻すと読めなくなること、公開中の差し替えで外れた画像が使用していない状態に戻ることを確かめる
  - 使用中の画像を学生団体が更新・削除できないこと (M-E05、ID 指定と where 指定の両方)、他人の画像 ID を自分の企画に保存すると M-E17 になること、他人の画像の読み取り・更新・削除が拒否されることを確かめる
  - 配信エンドポイントで未使用画像が 404 になること、所有者なしの既存画像が未認証に読めて学生団体の一覧に出ないこと、実行委員が全件を扱えることを確かめる
  - 完了状態: メディアの公開範囲に関する結合テストが 3 者について通る
  - _Requirements: 7.9, 7.10, 7.11, 9.1, 9.2, 9.3, 9.4, 9.5, 9.6, 9.7, 9.8, 9.9, 10.2_
  - _Boundary: media-publication.int.test, media.int.test_
  - _Depends: 1.1, 4.3, 5.2, 6.1, 8.1_
- [ ] 8.4 (P) 招待の発行・再送・失敗を結合テストで検証する
  - 送信は `vi.spyOn(payload, 'sendEmail')` で捕捉する (`payload/dist/index.js:416-417` で `sendEmail` はインスタンスのプロパティとして定義されている)。送信失敗は `mockRejectedValueOnce` で作る
  - CI に `EXHIBITOR_CONTACT_URL` が無いため、`vi.stubEnv` で与える
  - ジョブはテストから `payload.jobs.run()` を呼んで実行する (テスト中は autoRun しない)
  - Local API と REST 相当で学生団体を作成すると 1 通送られ、本文にパスワードが含まれないことを確かめる
  - 再送で旧トークンのパスワード設定が M-E06 になること、送信が失敗しても作成が成功して「送信失敗」になること、期限切れトークンで M-E06 になることを確かめる。期限切れは `payload.db.updateOne` で `resetPasswordExpiration` を過去の日時に書き換えて作る
  - 作成が巻き戻ったとき (例: 作成の後段で例外) にジョブが残らず、送信されないことを確かめる
  - 完了状態: 招待に関する結合テストが通る
  - _Requirements: 4.1, 4.2, 4.5, 4.6, 4.10, 4.11, 4.12, 4.13, 4.14, 10.2_
  - _Boundary: invitation.int.test_
  - _Depends: 4.3, 7.7_
- [ ] 8.5 全体の検証と本番反映前の確認
  - CMS の型チェック・テスト・ビルドを通し、スキーマ変更が追加だけで破壊的変更検出に掛からないことを確かめる
  - ローカル管理画面で学生団体としてログインし、ナビに学生企画とメディアだけが出ること、マップ配置エリアの名前と出演枠の表が表示されること、公開済み企画が読み取り専用になること、アカウント画面でパスワードを変更できること、パスワード設定画面で期限切れの案内が出ること、実行委員として管理画面からユーザーを新規作成するときにパスワードを入力せずに保存でき、招待メールが送られる (ローカルはコンソール出力) ことを確かめる
  - ローカルは `infisical run --env=prod -- pnpm dev` で起動する。`SMTP_HOST` が入らないため SMTP は無効になり、メールはコンソール出力になる。リンクは 7.1 の手順で取り出す
  - 本番反映後、タスク 2.1・2.2・2.4 の反映を前提に、テスト用の学生団体アカウントへ招待メールが届くことを確かめる
  - 完了状態: CI 相当の検証が全て通り、手動確認の項目が全て期待どおりである
  - _Requirements: 2.1, 2.3, 4.7, 4.13, 4.14, 4.17, 7.2, 7.12, 10.1, 10.3_
  - _Depends: 2.1, 2.2, 2.4, 8.2, 8.3, 8.4_

- [ ] 9. (P) 運用手順書に実行委員の手順を追記する
  - 学生団体アカウントの発行 (管理画面と API、パスワード欄は自動入力されるため触らないこと、招待メールの自動送付、招待メール列の見方、送信失敗・期限切れ・紛失時の再送) を書く
  - 受け皿レコードの作成と割当を、アカウント作成 → 企画作成 (所有者・団体名・カテゴリ・エリア・ブース番号・マップ表示ラベル) → ステージ出演枠作成の順で、管理画面と API の双方について書く
  - 招待リンクの有効期限は発行 (再送を含む) から 72 時間で、期限が切れたら再送することを書く
  - 学生団体本人のパスワード再設定 (リンクは 1 時間有効)、学生団体から見た操作範囲と公開までの流れ、一覧を公開状態で絞り込んで確認・公開する手順を書く
  - 公開後の修正依頼 (下書きに戻す間は公式サイトから消えることを明記)、公開前の修正依頼のシステム外の連絡手段、既存の OIDC 学生団体アカウントのローカル認証への移行を書く
  - 既存の記述で今回の変更と食い違う箇所を書き換える: `:57` (ロール割り当ては IdP のグループで自動反映)、`:168-170` (ロール写像が参照するグループに `student_exhibitor` がある)、`:180-200` (ロールごとの見え方の表と説明。他者の公開済み企画が一覧に出る、出展者が企画を作成できる等)
  - ローカル開発の節 (`:66`) に、`SMTP_HOST` が無いとメールはコンソール出力になることと、7.1 のリンク取り出し手順を足す
  - 環境変数の表 (`:80-90`) と、Infisical に登録が必要なシークレットの表 (`:154-161`) に、`SMTP_HOST` / `NOREPLY_SMTP_PASSWORD` / `EXHIBITOR_CONTACT_URL` を足す
  - 完了状態: `docs/cms-operations.md` に要件 6 の 7 項目が全て記載され、既存の記述に今回の変更との食い違いが残っていない
  - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5, 6.6, 6.7_
  - _Boundary: docs/cms-operations.md_
  - _Depends: 5.2, 7.1, 7.7_
