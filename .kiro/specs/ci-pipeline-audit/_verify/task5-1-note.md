# Task5.1 検証用ノート

このファイルは additive-schema-check-dummy.yml が `directus/schema/snapshot.yaml`
を変更しない PR で `Detect breaking snapshot.yaml changes` context を success
報告することを確認するためのテスト用変更である。マージはせず、確認後に閉じる。
